server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://mysql-prod:3306/ecommerce_auth
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate

    show-sql: false

    open-in-view: false